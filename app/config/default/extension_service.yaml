apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: app
    app.kubernetes.io/managed-by: kustomize
  name: controller-manager-extension-service
  namespace: system
spec:
  ports:
  - name: https
    port: 9999
    protocol: TCP
    targetPort: 9999
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: app
